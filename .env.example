# Example environment variables for server
# Update these values for your local environment

# PostgreSQL connection string (required)
# e.g. postgresql://user:password@localhost:5432/seatmaster
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/seatmaster

# Optional: port the server will listen on (default 5000)
PORT=5000
